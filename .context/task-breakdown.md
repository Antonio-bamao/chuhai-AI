# 任务拆解

## 里程碑 1：Walking Skeleton

### 任务卡 M1-01：资产备份与原始启动记录

- 目标：在不修改原始资产的前提下，建立备份点并记录客户端原始启动行为。
- 输入：仓库根目录中的安装包、DLL、`data/`、图标和配置资源。
- 输出：备份清单、资产清单草稿、原始启动行为记录。
- DoD（可自检的完成定义）：能说明每个关键资产的位置和备份位置；原始客户端启动结果有截图、日志或文字记录；未修改原始文件。
- 明确不做：不反编译、不 patch、不联网压测、不碰服务器。
- 规模（S·M·L）：S

### 任务卡 M1-02：JADX 导出源码树

- 目标：把客户端 Java/JAR 产物导出为可浏览源码树。
- 输入：M1-01 备份后的客户端产物。
- 输出：反编译源码目录、反编译命令记录、初步入口类候选。
- DoD（可自检的完成定义）：源码树可打开；能定位或列出 `StartApp`、`JLoginNew`、`ClawWorkspace` 等候选路径；不修改 native 组件。
- 明确不做：不做字符串解密、不改授权逻辑、不做 UI 删除。
- 规模（S·M·L）：M

### 任务卡 M1-03：原始 JAR/客户端可运行验证

- 目标：确认未改动产物在隔离环境中能运行，并记录启动到鉴权阻断的路径。
- 输入：原始备份产物、隔离环境、启动日志或控制台输出。
- 输出：`verify.log` 初版，包含 before 行为和鉴权阻断位置线索。
- DoD（可自检的完成定义）：能复现原始启动；能描述启动成功或失败发生在哪一环；无服务器探测或压测行为。
- 明确不做：不尝试绕过授权，不制造大量失败请求。
- 规模（S·M·L）：S

## 后续里程碑任务卡

### M2：字符串解密

- 目标：批量产出 `string_map.json`，让授权相关关键词可检索。
- 输入：反编译源码树、解密函数候选、运行时上下文。
- 输出：密文到明文映射、关键类明文化记录。
- DoD（可自检的完成定义）：关键入口明文可读；失败路径明确切换到动态 dump。
- 明确不做：不 patch 授权，不动业务联网。
- 规模（S·M·L）：M

### M3：授权接缝清单

- 目标：定位授权、时效、支付状态判定点。
- 输入：`string_map.json`、调用链、抓包关键词。
- 输出：`seams.md`，包含类.方法、返回类型、原逻辑、拟改动、回滚点。
- DoD（可自检的完成定义）：接缝精确到行或可 patch 的方法级别。
- 明确不做：不实际改动产物。
- 规模（S·M·L）：M

### M4A-01：九产品本地兼容目录

- 状态：完成（v40 已通过运行时九产品卡与原始 logo 验收）。
- 目标：从原包证据恢复 8 个可进入产品和 1 个未开通独立站产品。
- 输入：`m4-product-menu-evidence.md`、主 logo、产品图标、`product-selector.html` 消费契约。
- 输出：集中定义的九产品兼容数据与字段证据表。
- DoD（可自检的完成定义）：产品 code 为 `whatsapp/tiktok/facebook/instagram/twitter/telegram/geo/wskefu/aishope`；前 8 个可进入，`aishope` 未开通；不存在 v33 临时 AIGC 产品值；未知数值字段均标“恢复值”且可集中替换。
- 明确不做：不请求或伪造远端 token，不把恢复 ID 描述成原始真实 ID。
- 规模（S·M·L）：M

### M4A-02：各系统侧边栏兼容菜单

- 状态：完成（76 项目录进入测试；WhatsApp 11 项菜单与原始 icon 已通过宿主截图验收）。
- 目标：使用原包 icon 族、i18n 文案和 Java 分发证据恢复各系统菜单拓扑。
- 输入：`i18.cnf`、SVG 菜单资源、`JSBFMain`/菜单模型消费点、用户截图验收结构。
- 输出：每个产品独立、稳定、可替换 ID 的菜单树。
- DoD（可自检的完成定义）：每个可进入系统显示对应菜单族；截图只用于核对显示结构；未知内部 ID 标“恢复值”；临时 `C28500001/C28500002` 菜单撤除。
- 明确不做：不根据截图文字反推原始 `id/parentId/localCode/linkUrl`。
- 规模（S·M·L）：L

### M4B-01：免操作启动与本地兼容服务正式化

- 状态：进行中（免登录、产品选择器、主界面和黑洞代理启动已通过；尚缺物理断网与最终双击包）。
- 目标：在不破坏 v33 的前提下，让双击启动直接建立本地永久授权态并进入产品/主界面。
- 输入：v33 基线、登录/getInfo/产品/菜单/Web bridge 接缝。
- 输出：正式补丁产物、diff、回滚点、双击与断网验证记录。
- DoD（可自检的完成定义）：无需填写账号或点击登录；断网可进入九产品选择器和主界面；授权外表恒为已授权/永久。
- 明确不做：不恢复 `offline-home.html` 为业务入口，不回退真实 URL、软件渲染、Web 首屏和 `im.port.udp` 修复。
- 规模（S·M·L）：M

### M5A：业务依赖分类与直连回归

- 状态：进行中（第一版静态依赖分类、第一轮只读验收、菜单路由发现和 WhatsApp `AI采集` 页面层恢复值验证已完成；真实业务动作尚未执行）。
- 目标：逐项判定业务属于客户端直连第三方、原后端代理、原后端数据库、云资源/算力或 native 依赖。
- 输入：M4B 产物、抓包、调用链和低风险业务流程。
- 输出：业务依赖矩阵与可直连功能回归记录。第一版静态矩阵和只读验收记录见 `.context/m5a-business-dependency-inventory.md`，菜单真实路由发现见 `.context/m5a-menu-route-discovery.md`。
- DoD（可自检的完成定义）：至少跑通一个直连或低风险流程；每个模块有依赖类别和下一步；不执行真实群发、支付、批量采集、上传或云设备创建等有副作用动作。
- 明确不做：不把失败接口直接本地伪成功，不压测，不泛化代理全部 `/prod-api/*`。
- 当前新前置：v40 统一 `/pc/aicloud/my` 菜单入口不足以验收 WhatsApp/GEO 真实业务。已确认菜单分发依赖 `localCode/linkUrl`，且原客户端支持 `ZWBrowser/JBigDataMaster/ai_mnq_manager/PhoneFission/JSinglepage` 等打开器；已进一步发现 `data/app/res/spider/*.cnf` 本地采集脚本、`SBFApi.H(String)` 远端优先/本地兜底配置契约，以及 `/pc/dataCollect/collectionTask/data_index?spiderCode=...&moduleCode=...` 数据采集入口族。v43-v47 已把 WhatsApp `C4749_006 / AI采集` 收敛为父菜单 + 恢复值子路由 `REC_WHATSAPP_COLLECT_USERS_ROUTE`，并只读验证可加载 dataCollect 页面层；当前阻断为 `mijava` bridge 与 `/prod-api/getInfo/getRouters` 后续依赖。下一步只读分析该页面 chunk/bridge/API 契约，不提交任务。
- 规模（S·M·L）：L

### M5B：失效原后端接口兼容重建

- 目标：对 M5A 证明依赖遗失服务端的功能逐接口重建兼容后端。
- 输入：请求/响应契约、持久化需求、队列/云资源需求。
- 输出：模块化兼容服务、测试、迁移与回滚说明。
- DoD（可自检的完成定义）：每个模块独立验收，不扩大到无证据接口。
- 明确不做：不推倒重写整个客户端或后端平台。
- 规模（S·M·L）：L

### M6：业务模块文档

- 目标：把逆向理解整理成人话版业务模块文档。
- 输入：源码树、明文映射、调用链、验证记录。
- 输出：`assets.md`、模块说明、后续重构建议。
- DoD（可自检的完成定义）：每个主要模块有职责、入口、依赖和风险说明。
- 明确不做：不启动全量重写。
- 规模（S·M·L）：M

## 依赖关系

- M1 是所有后续工作的入口。
- M2 依赖 M1 的源码树和入口候选。
- M3 依赖 M2 的明文映射。
- M4 依赖 M3 的精确接缝清单。
- M4A 依赖 M3 与原包资源证据；M4B 依赖 M4A 的稳定产品/菜单契约。
- M5A 依赖 M4B 的正式补丁产物；M5B 只处理 M5A 证实失效的依赖。
- M6 依赖 M2 到 M5B 形成的源码理解和验证记录。

## 范围外

- 修改、探测或压测服务器。
- 修改 `libmytrpc`、FFmpeg、VECore 等 native 组件。
- 通过 hosts 或全局封网方式粗暴断网。
- 删除登录 UI 或大面积重构启动链路。
- 完全离线化、全量重写、公开传播或商用。
- 继续消耗时间猜测、爆破或伪造原远端服务器认可的 token。
